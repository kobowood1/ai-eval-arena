{
  "name": "arena",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,runtime -c magenta,cyan \"npm run dev --workspace apps/web\" \"npm run dev --workspace apps/runtime\"",
    "build": "npm run build --workspace packages/lib && npm run build --workspace packages/db && npm run build --workspace packages/providers && npm run build --workspace apps/web && npm run build --workspace apps/runtime",
    "check": "npm run typecheck --workspaces --if-present && npm run lint --workspaces --if-present && npm run test --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "db:generate": "npm run db:generate --workspace packages/db",
    "db:migrate": "npm run db:migrate --workspace packages/db"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}
